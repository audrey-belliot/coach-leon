<div class="container">
  <div class="d-flex align-items-baseline">
    <%= link_to :back, class: "text-decoration-none text-muted" do %>
      <i class="fa-solid fa-chevron-left pe-3"></i>
    <% end %>
    <h1 class="mb-4">Toutes les<br><strong>activités</strong></h1>
  </div>

    <div class="cards-grid">
      <% @activities.each do |activity| %>
          <div class="card">
            <p class="card-text-home">
              <%= activity.activity %>

              <%= button_to "Ajout", new_activity_activities_log_path(activity),
                        method: :get,
                        class: "btn btn-blue"
              %>
            </p>
          </div>

      <% end %>
    </div>

<div class="mt-2">
      <button data-recipe-category-target="cardio" data-action="click->recipe-category#loadPetitdej" class="btn-orange-mini">Cardio</button>
      <button data-recipe-category-target="muscu" data-action="click->recipe-category#loadDej" class="btn-white-grey">Musculation</button>

      <div data-recipe-category-target="petitdejSection" class="mt-3">
        <div class="cards-grid">
          <% @cardio.each do |cardio| %>
            <div class="card">
            <p class="card-text-home">
              <%= cardio.activity %>
              <%= button_to "Ajout", new_activity_activities_log_path(cardio),
                method: :get,
                class: "btn btn-blue"
              %>
            </p>
          </div>
          <% end %>
        </div>
      </div>

</div>

</div>
